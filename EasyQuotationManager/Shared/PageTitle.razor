@code{
    [Parameter]
    public string Title { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await _IJSRuntime.InvokeVoidAsync("BlazorSetTitle", Title + " - Easy Quotation Manager");
    }
}
